// This interface is preserved for type compatibility, though client no longer calculates levels.
export interface ExperienceLevel {
  level: number;
  totalExperience: number;
  experienceToLevelUp: number;
}
